# Risk Policy Configuration
max_daily_loss_pct: 0.05
max_symbol_risk_pct: 0.15
kelly_fraction: 0.25
vol_target_ann: 0.20
max_concurrent_positions: 10
min_confidence: 0.01  # Very low threshold to allow signals